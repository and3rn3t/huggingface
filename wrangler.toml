name = "hug"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Environment variables for Pages Functions
# Note: Secrets should be set via Cloudflare dashboard or wrangler CLI
# These are non-sensitive defaults
[vars]
# VITE_HF_TOKEN should be set as a secret in Cloudflare dashboard

# For local development with wrangler pages dev
[env.preview]
compatibility_date = "2024-01-01"
